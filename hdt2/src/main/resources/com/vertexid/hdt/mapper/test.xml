<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vertexid.hdt.ExampleDao">
 
  <select id="getUserName" parameterType="string" resultType="string">
    select user_name as name from User where userid= #{userId}
  </select>
  
  <select id="insertUser" parameterType="com.vertexid.hdt.UserVo">
    insert into User(userid,ext_auth,password_hash,role,user_name) 
    values(#{userId},#{extAuth},#{passwordHash},#{role},#{userNmae});
  </select>
  
  <select id="udpateUser" parameterType="com.vertexid.hdt.UserVo">
    update User
	   set ext_auth = #{extAuth}
	     , password_hash = #{passwordHash}
		 , role = #{role}
		 , user_name = #{userName}
	 where userid = #{userId}	 
  </select>
  
  <select id="deleteUser" parameterType="com.vertexid.hdt.UserVo">
    delete User where userid= #{userId}
  </select>
  
  <select id="selectUser" parameterType="com.vertexid.hdt.UserVo" resultType="com.vertexid.hdt.UserVo">
    select userid as userId
         , ext_auth as extAuth
         , password_hash  as passwordHash
         , role as role
         , user_name as userName
      from User where userid= #{userId}
  </select>
</mapper>